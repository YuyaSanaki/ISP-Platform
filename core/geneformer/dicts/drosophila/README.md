# Fly symbol → FBgn lookup for ISP / perturbation when input uses gene symbols.

Tab-separated `symbol` → `fbgn_id`. Used by `load_input_symbol_table()` when
`species.model_organism` is `drosophila`.

Extend this file for fly studies that use symbols instead of `FBgn` IDs in configs.
